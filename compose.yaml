services:
  miopia-api:
    build: .
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    ports:
      - "127.0.0.1:8000:8000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      MIOPIA_LOG_LEVEL: WARNING
